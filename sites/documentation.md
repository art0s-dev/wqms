# Sites
these sites here are used as test sites for testing the 
meta extractor. In the testsuite there is a website factory,
which loads the sites and puts the contents into a website object that
is fed to the meta extractor.

To add more sites, just add the site to the according folder
and add a descriptive name to the Test site Class.

This folder is also used by the file reader tests for the toolbox